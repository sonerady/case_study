import React from "react";
import { Logo } from "../../assets";
import styles from "./loading.module.scss";
const Loading = () => {
  return <div className={styles.spinner_container}>Loading...</div>;
};

export default Loading;
